# Cross-platform compatibility
target_platform=
target_arch=
# Ensure legacy peer deps for Next.js compatibility
legacy-peer-deps=true
# Disable optional dependencies that might be platform-specific
optional=false 